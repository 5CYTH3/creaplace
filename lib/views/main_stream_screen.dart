import 'package:flutter/material.dart';

class MainStreamScreenBody extends StatelessWidget {
  const MainStreamScreenBody({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}